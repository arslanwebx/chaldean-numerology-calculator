import { ContentPage } from "@/components/page";import { ContactForm } from "@/components/contact-form";import { siteConfig } from "@/config/site";import { pageMetadata } from "@/lib/metadata";
export const metadata=pageMetadata("Contact","Ask a question, request a correction, or report an accessibility issue without sharing calculator data.","/contact/");
export default function Page(){return <ContentPage eyebrow="Get in touch" title="Contact" intro="Ask about the documented method, suggest a correction, or report an accessibility or technical problem. Please never send a birth date or numerology result." path="/contact/"><h2>Contact by email</h2>{siteConfig.contactEmail?<p>Email <a href={`mailto:${siteConfig.contactEmail}`}>{siteConfig.contactEmail}</a>.</p>:<p className="notice">The owner has not configured a public contact email yet. Set <code>CONTACT_EMAIL</code> before launch to show the address and enable a reliable fallback.</p>}<h2>Send a message</h2><p>The form delivers only when the optional SMTP environment variables are configured. Your name, email, subject, and message are used to respond to your request.</p><ContactForm/></ContentPage>}

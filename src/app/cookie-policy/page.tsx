import { ContentPage } from "@/components/page";import { siteConfig } from "@/config/site";import { pageMetadata } from "@/lib/metadata";
export const metadata=pageMetadata("Cookie Policy","Understand essential, preference, analytics, and advertising cookies and how to manage your choices.","/cookie-policy/");
export default function Page(){return <ContentPage eyebrow={`Last updated ${siteConfig.policyDate}`} title="Cookie Policy" intro="The calculator does not need cookies to process a name or birth date. Optional services remain disabled until deliberately configured." path="/cookie-policy/"><h2>Essential technology</h2><p>Basic page delivery, security, and contact-form protection may use short-lived technical state where the hosting environment requires it. Calculator entries and results are not stored in cookies.</p><h2>Preferences</h2><p>If non-essential services are enabled in the future, a preference cookie may remember whether you accepted or rejected those services. It should contain the choice, not calculator data.</p><h2>Analytics and advertising</h2><p>Analytics cookies are not set while analytics is disabled. Advertising cookies are not set by this site while advertising is disabled. If either service is enabled, this policy and consent interface must identify the vendors and choices before applicable scripts load. Third-party vendors remain responsible for their own technology and policies.</p><h2>Managing cookies</h2><p>You can reject non-essential services in the consent interface when one is active, reopen privacy choices from the footer, or use browser controls to delete and block cookies. Blocking essential storage may affect features that genuinely require it, but it will not prevent the local numerology calculation.</p></ContentPage>}

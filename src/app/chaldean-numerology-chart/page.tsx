import { ContentPage, CalculatorCta } from "@/components/page";
import { CHALDEAN_GROUPS } from "@/data/chaldean";
import { pageMetadata } from "@/lib/metadata";
export const metadata = pageMetadata("Chaldean Numerology Chart: Letter Values 1 to 8", "Use the complete Chaldean letter chart, learn how names are normalized, and follow transparent worked examples.", "/chaldean-numerology-chart/");
export default function Page(){return <ContentPage eyebrow="Letter reference" title="Chaldean Numerology Chart" intro="This chart assigns the Latin alphabet to values 1 through 8. Use it to check every step of a Chaldean name calculation." path="/chaldean-numerology-chart/" sections={[{id:"chart",label:"The chart"},{id:"input",label:"Name handling"},{id:"examples",label:"Worked examples"}]}><h2 id="chart">The complete 1-to-8 chart</h2><div className="table-wrap" role="region" aria-label="Complete Chaldean alphabet chart" tabIndex={0}><table><thead><tr><th>Number</th><th>Letters</th></tr></thead><tbody>{Object.entries(CHALDEAN_GROUPS).map(([n,l])=><tr key={n}><td>{n}</td><td>{l.join(" ")}</td></tr>)}</tbody></table></div><p>There is no letter group for 9 in the commonly published Chaldean chart. The value is traditionally treated as set apart, but it remains a valid reduced result. For example, a total of 18 becomes root 9.</p><h2 id="input">How names are handled</h2><p>Capitalization does not affect a result. Common accented Latin characters are normalized to their base letter, so É uses E and Ñ uses N. Spaces, hyphens, apostrophes, periods, and ordinary punctuation add nothing, although the calculator keeps word boundaries so it can show useful subtotals.</p><p>The chart does not define Arabic, Urdu, Devanagari, Chinese, Cyrillic, or other writing systems. Automated transliteration can change spelling choices that matter to a letter total, so the calculator asks you to enter the usual Latin transliteration you personally use.</p><h2 id="examples">Worked examples</h2><h3>ALICE</h3><p>A1 + L3 + I1 + C3 + E5 = 13. Then 1 + 3 = 4, displayed as <strong>13/4</strong>.</p><h3>JOHN</h3><p>J1 + O7 + H5 + N5 = 18. Then 1 + 8 = 9, displayed as <strong>18/9</strong>.</p><h3>CHALDEAN NUMEROLOGY</h3><p>The word subtotals are 30 and 40. Together they make 70, which reduces to 7 and is displayed as <strong>70/7</strong>.</p><p>A compound should be preserved before reduction. This calculator does not stop at 11, 22, or 33 because that master-number rule belongs to other modern approaches unless explicitly stated.</p><CalculatorCta/></ContentPage>}

import { ContentPage } from "@/components/page";import { siteConfig } from "@/config/site";import { pageMetadata } from "@/lib/metadata";
export const metadata=pageMetadata("Accessibility Statement","Accessibility features, current targets, known limitations, and how to report a barrier.","/accessibility/");
export default function Page(){return <ContentPage eyebrow="Inclusive access" title="Accessibility Statement" intro="The site aims for WCAG 2.2 Level AA and is designed for keyboard, screen-reader, touch, zoom, and reduced-motion use." path="/accessibility/"><h2>Measures in the site</h2><p>Pages use semantic landmarks, a skip link, logical headings, visible focus indicators, labeled fields, connected error messages, concise live announcements, keyboard-operable menus, accessible native disclosure controls, and text alternatives where graphics communicate information. Layouts reflow on narrow screens and support browser zoom without fixed text containers.</p><h2>Calculator access</h2><p>The local calculator can be submitted with Enter. Errors explain how to fix a field and are programmatically connected to it. A successful result receives keyboard focus while the live region announces only a concise summary, avoiding an unexpectedly long screen-reader interruption.</p><h2>Motion and contrast</h2><p>Animation is not required to understand content, and reduced-motion preferences are respected. The visual system uses strong text and control contrast, with gold reserved for short accents rather than body copy.</p><h2>Ongoing improvement</h2><p>Automated checks help but cannot cover every assistive-technology combination. If you encounter a barrier, include the page, task, browser, and assistive technology if you are comfortable sharing them. Do not include calculator inputs.</p>{siteConfig.contactEmail?<p>Report a barrier at <a href={`mailto:${siteConfig.contactEmail}`}>{siteConfig.contactEmail}</a> or through the contact form.</p>:<p>Use the contact form. A direct email address will also appear after <code>CONTACT_EMAIL</code> is configured.</p>}</ContentPage>}
